apiVersion: v1
kind: Pod
metadata:
  name: heater
spec:
  containers:
    - name: heater
      image: ninckblokje/heater
      imagePullPolicy: Always
      resources:
        limits:
          memory: 256Mi
        requests:
          memory: 256Mi